{
  "name": "mock-config-server",
  "version": "1.0.0",
  "description": "Custom mock configuration server for client bootstrap",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
